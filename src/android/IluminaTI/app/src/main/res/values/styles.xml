<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="android:actionBarStyle">@style/ActionBarTheme</item>
        <item name="android:buttonStyle">@style/ButtonTheme</item>
        <item name="android:windowBackground">@color/activityBackground</item>
    </style>

    <style name="ActionBarTheme" parent="@android:style/Widget.Holo.ActionBar.Solid">
        <item name="android:background">@color/actionBar</item>
    </style>

    <style name="ButtonTheme">
        <item name="android:background">@drawable/button_theme</item>
        <item name="android:textColor">@color/buttonText</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">@dimen/buttons_text_size</item>
        <item name="android:layout_marginBottom">@dimen/buttons_margin_bottom</item>
    </style>

    <style name="ActionBarTheme.Text">
        <item name="android:color">@color/actionBarText</item>
    </style>

</resources>
